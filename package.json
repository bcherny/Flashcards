{
  "name": "flashcards",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "private": true,
  "scripts": {
    "dev": "concurrently \"yarn --cwd 'client' dev\" \"yarn --cwd 'server' dev\""
  },
  "devDependencies": {
    "concurrently": "^6.5.0"
  }
}
